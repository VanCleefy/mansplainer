// SPDX-License-Identifier: MIT
pragma solidity >= 0.7.0 < 0.9.0;
//Remove the /* and */ tags to de-comment the code
/*
//contract name {

    tod = "type of data to be stored into the array (ex: uint, string, bool)"
    [] = "the type of data structure, in this case [] is the array data structure"
    keyword = "scope (ex: public, private, view, etc)"
    name = "name of the array"
    valX = "value, (ex: 1, 7, random-string, fda3fa3)"

    tod [] keyword name;  //initializing an array with no values
    tod [] keyword name = [val1, val2, valN]; //initializing with values
    
    
    example of array:

    uint [] public numberList = [1,2,3,5];

//}
*/